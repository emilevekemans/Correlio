##BACKEND

#créer l'environnement (venv)
python -m venv .venv #créer l'environnement virtuel

#désactiver l'environnement venv
deactivate

#ouvrir/activer le back end en local
.\.venv\Scripts\activate

#lancer le backend local
uvicorn api:app --reload --port 8000

#vérifier que le backend tourne (en local)
http://localhost:8000/health

http://localhost:8000/assets

http://localhost:8000/docs



##GIT/GITHUB
git status
git add .
git commit -m "Message clair"
git push 

#lister les versions sauvées
git log --oneline

#aller voir une des version
git checkout '50ed516'

#revenir a la version actuelle sans prendre en compte ce que je vais voir dans le passer
git checkout main

#revenir a la version précédente visualisée ()
git revert d5ed516

test
test git



##FRONTEND

#lancer le front end local
npm run dev

